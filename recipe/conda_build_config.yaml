channel_sources:                                                        # [win]
  - conda-forge/label/numpy_rc,conda-forge/label/meson_dev,conda-forge  # [win]

blas_impl:
  - accelerate   # [osx]
  - netlib
  - openblas     # [not (x86_64 and not osx)]
  - blis         # [x86_64]
  - mkl          # [x86_64 and osx]
  - openblas     # [x86_64 and not osx]
  - openblas     # [x86_64 and not osx]
  - mkl          # [x86_64 and not osx]
  - mkl          # [x86_64 and not osx]

enforce_avx512:
  - None         # [osx]
  - None
  - None         # [not (x86_64 and not osx)]
  - None         # [x86_64]
  - None         # [x86_64 and osx]
  - present      # [x86_64 and not osx]
  - absent       # [x86_64 and not osx]
  - present      # [x86_64 and not osx]
  - absent       # [x86_64 and not osx]

zip_keys:
  - - blas_impl
    - enforce_avx512
